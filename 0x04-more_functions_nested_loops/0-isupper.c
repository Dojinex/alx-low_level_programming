#include "main.h"

/**
 * _isuper - check if a letter is uppercase character
 * @x: The number to be checked
 *
 * Return: 1 for upper or 0 for any elsse
 */
int _isuper(int x)
{
if (x >= 65 && x <= 90)
{
return (1);
}
return (0);
}
